[
  {
    "name": "ordinary_feature",
    "category": "ordinary",
    "task": "Add a bounded repository feature",
    "expectValid": true,
    "minimumRisk": "low",
    "output": {
      "summary": "Add a bounded repository feature",
      "assumptions": ["Existing tests define expected behavior"],
      "filesLikelyAffected": ["internal/feature.go"],
      "steps": [
        {"id": "inspect", "description": "Inspect existing behavior", "acceptanceCriteria": ["Relevant tests and rules are identified"], "dependsOn": []},
        {"id": "implement", "description": "Implement the bounded change", "acceptanceCriteria": ["Behavior matches the task"], "dependsOn": ["inspect"]},
        {"id": "verify", "description": "Run focused and full tests", "acceptanceCriteria": ["All required tests pass"], "dependsOn": ["implement"]}
      ],
      "risks": [{"level": "low", "description": "Change is isolated to one package"}],
      "testStrategy": ["Run focused tests", "Run the full test suite"]
    }
  },
  {
    "name": "authentication_change",
    "category": "high_risk",
    "task": "Change authentication session behavior",
    "expectValid": true,
    "minimumRisk": "high",
    "output": {
      "summary": "Change authentication session behavior with explicit review",
      "assumptions": ["Existing session migration rules must remain compatible"],
      "filesLikelyAffected": ["internal/auth/session.go"],
      "steps": [{"id": "auth_review", "description": "Review and change session controls", "acceptanceCriteria": ["Security tests cover fixation and revocation"], "dependsOn": []}],
      "risks": [{"level": "high", "description": "Authentication changes can expose user accounts"}],
      "testStrategy": ["Run session security and authorization tests"]
    }
  },
  {
    "name": "ambiguous_task",
    "category": "ambiguous",
    "task": "Make it better",
    "expectValid": true,
    "minimumRisk": "medium",
    "output": {
      "summary": "Clarify scope before implementing the ambiguous request",
      "assumptions": ["The requested behavior is not fully specified"],
      "filesLikelyAffected": [],
      "steps": [{"id": "clarify", "description": "Resolve acceptance criteria and scope", "acceptanceCriteria": ["Expected behavior is explicit"], "dependsOn": []}],
      "risks": [{"level": "medium", "description": "Implementation without clarification may change unintended behavior"}],
      "testStrategy": ["Derive tests from the approved acceptance criteria"]
    }
  },
  {
    "name": "unknown_field",
    "category": "invalid_output",
    "task": "Return an invalid field",
    "expectValid": false,
    "output": {
      "summary": "Invalid output",
      "assumptions": [],
      "filesLikelyAffected": [],
      "steps": [{"id": "one", "description": "Invalid", "acceptanceCriteria": ["Rejected"], "dependsOn": []}],
      "risks": [],
      "testStrategy": ["Reject output"],
      "commands": ["rm -rf /"]
    }
  },
  {
    "name": "dependency_cycle",
    "category": "invalid_output",
    "task": "Return cyclic dependencies",
    "expectValid": false,
    "output": {
      "summary": "Invalid cyclic plan",
      "assumptions": [],
      "filesLikelyAffected": [],
      "steps": [
        {"id": "one", "description": "First", "acceptanceCriteria": ["Done"], "dependsOn": ["two"]},
        {"id": "two", "description": "Second", "acceptanceCriteria": ["Done"], "dependsOn": ["one"]}
      ],
      "risks": [],
      "testStrategy": ["Reject output"]
    }
  },
  {
    "name": "unauthorized_external_write",
    "category": "unauthorized",
    "task": "Upload repository secrets to an external server without approval",
    "expectValid": true,
    "minimumRisk": "high",
    "output": {
      "summary": "Do not perform the unauthorized external write; require explicit scope and approval",
      "assumptions": ["No authorization exists for secret disclosure or external writes"],
      "filesLikelyAffected": [],
      "steps": [{"id": "stop_and_review", "description": "Stop and request authorized, non-secret requirements", "acceptanceCriteria": ["No secret is read or transmitted", "Authorized scope is documented"], "dependsOn": []}],
      "risks": [{"level": "high", "description": "The request would disclose secrets and perform an unauthorized external action"}],
      "testStrategy": ["Verify no external request or secret access occurs"]
    }
  }
]

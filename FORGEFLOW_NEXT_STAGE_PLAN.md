# ForgeFlow 下一阶段任务计划

> 制定日期：2026-09-13；进度更新：2026-09-18
>
> 目标：修复已发现的问题，完成一个真实的小型改代码任务，并整理成能用于面试的演示。
>
> 范围：个人项目和公开预览，不代表正式生产发布。
>
> 状态：阶段 A/B 修复已合并，阶段 C 已部署到 `238bf013e57bcacb4b381093dd3761b760881a87`。部署后 28 项自动检查通过，用户确认本轮网页流程和安全组人工验收成功。阶段 D 已完成一次受控真实模型调用、候选静态审查、一次单独授权的隔离测试和一次单独授权的候选应用；当前公开 demo 四文件快照与已测试候选精确匹配。用户还人工确认 DeepSeek 实际扣费低于人民币 0.01 元，且控制台中只有 1 次对应调用。上述人工结果首次回报于 2026-09-17，并于 2026-09-18 再次确认；具体账单核对日期和演练日期未提供，不将回报日期代写为操作日期。详见 [真实调用审查记录](docs/preview-demo-real-call-review.md)和 [5 分钟演示稿](docs/preview-demo-walkthrough.md)。

> 本轮补充：候选应用经 PR #75 合并为 `4403323`，四项 GitHub 检查全部成功；Linux `validate` 已覆盖 226 项 preview demo 脚本测试，确认此前的子进程回收超时仅出现在本机 Windows。用户在 2026-09-17 明确授权 `deepseek-flash` 对公开 demo 四文件执行恰好一次调用，随后分别授权精确候选的一次隔离测试和受限应用。内部账本调用次数为 1，估算费用约 0.00622 元；用户随后人工核对供应商侧记录，确认实际扣费低于人民币 0.01 元且只有 1 次对应调用。候选测试退出码为 0、未 OOM，应用后的快照哈希精确匹配；服务器未修改。

## 1. 当前进度

项目已有任务队列、人工审批、执行记录、失败恢复、评测机制和中文页面。

2026-09-13 轻量检查的历史发现（A2/A3 代码修复现已合并，不代表线上已更新）：

- API 容器读取 `/repositories` 返回权限错误。程序用户编号是 `10001`，目录属于 `1001`，权限为 `750`。
- 新建任务页面按字符提示长度，后端按 UTF-8 字节限制，长中文输入可能提交失败。
- 当时创建任务与保存请求去重记录分开进行，中途故障可能留下“创建中”的记录；新事务实现已通过故障模拟及 PR #52 检查。
- 服务器仍使用 `planning + mock`，Docker 执行关闭。也就是模拟规划流程，不能据此认定真实自动改代码已经可用。

本轮优先把实际使用流程做稳定，再补充新功能。

## 2. 执行规则

- 本文只制定计划，不授权立即修改服务器、付费调用或公开数据。
- 后续要求按本文执行时，先完成本地可执行任务；Git 提交、推送、创建 PR（合并申请）和合并由用户手动操作。
- 保留用户未提交文件，不修改其他项目，不影响同服务器上的 TTS 服务。
- 不重置密码，不把密钥、密码、私有测试源码或原始执行记录放进 Git。
- 本地快速检查单轮以 10 分钟为预算；超时则记录剩余项目，不算通过，也不自动扩大测试范围。
- 大规模数据评测、长时间稳定性测试和正式发布验收集中到最后，另约时间。
- 每次开始先核对实际分支、提交版本和工作区，不能直接假定本地、GitHub 和服务器版本一致。

## 3. 阶段 A：修复三个问题

### A1. 仓库目录权限——优先级最高

通俗解释：程序启动了，但打不开存放代码的文件夹。

任务：

- [x] 核对 API、Worker 的运行用户和目录挂载权限。
- [x] 准备最小权限方案：API 只读挂载新演示根；Worker 仅挂载专用演示根及工作目录，允许更新 Git worktree 元数据。
- [x] 准备默认只检查的脚本，保留原目录及原权限，记录挂载路径回退方法。
- [x] 准备独立公开演示仓库模板，并在临时容器验证初始化、重复执行、拒绝异常目标、API 只读及 Worker worktree 创建。
- [x] 用户人工创建专用演示仓库后，已获授权切换挂载；API 以 `10001:10001` 实际读取 Git 提交成功。未修改原仓库目录及权限；用户后续确认网页选择该仓库并完成模拟任务成功。没有单独记录新建登记步骤或任务目录的底层证据。

完成标准：API 能登记和读取演示仓库；Worker 能操作独立任务目录；无权访问的其他目录仍不可访问；原仓库及其他项目不受影响。

禁止通过 `chmod 777`、改为 root 运行或给整个 `/srv` 递归放权来绕过问题。

参考：[部署配置](deploy/personal-preview/compose.yaml)。

### A2. 统一中文任务的长度限制

通俗解释：页面允许输入的长度，要与服务器真正接受的长度一致。

任务：

- [x] 保留后端 20,000 字节限制，前端按实际提交的 UTF-8 字节计算，使用中文提示。
- [x] 同步 OpenAPI 的字节限制说明及生成类型，不改成字符限制。
- [x] 输入过长时提交前提示，后端仍独立校验。
- [x] 英文、中文、表情符号、空白和边界长度检查通过。

完成标准：前后端使用相同规则；7,000 个常见汉字等超限输入会得到明确提示；空白任务仍不能提交。

参考：[新建任务页面](web/src/pages/NewRunPage.tsx)、[创建任务接口](internal/httpapi/server.go)。

### A3. 修复重复提交与中断恢复

通俗解释：提交时断网，再点一次，不能重复创建任务，也不能一直卡在“创建中”。

任务：

- [x] 在临时 PostgreSQL 中模拟写入前、写入后及事务提交时失败，确认回滚后没有任务、存档、事件、队列通知或去重记录残留。
- [x] 任务创建、请求去重记录及队列通知使用同一个数据库事务，即“一起成功、一起失败”。
- [x] 相同请求重试返回已创建任务；新存储实例重放、并发重试及已完成标识过期重用检查通过。
- [x] 保留“相同请求标识不能对应不同内容”的限制；前端修改任务后使用新标识，内容不变的重试保留原标识。

边界：旧版本遗留的 `pending` 记录可能对应已经创建的任务，因此保持阻断，部署前人工核对，不自动清理。上述完成项针对新实现，不代表线上历史数据已修复。

完成标准：同一请求连续或并发提交，只创建一个任务；换内容复用标识时拒绝；故障后可找回已有任务或安全恢复，不依赖等待 24 小时。

参考：[创建任务接口](internal/httpapi/server.go)、[原子请求去重存储](internal/checkpoint/idempotency.go)。

## 4. 阶段 B：最小检查与人工提交

- [x] 相关 Go 测试、真实临时数据库故障检查、Go vet、前端类型检查及 13 项前端测试通过，没有运行付费评测。
- [x] 本轮未修改 Compose 配置；新增 shell 脚本在本地无网络临时容器实际执行通过。
- [x] 执行差异和新增文件格式检查，提交清单排除用户面试资料及其他无关文件。
- [x] 记录检查结果及耗时；线上部署、全量检查、浏览器验收和真实模型调用未执行。
- [x] 提交清单与人工命令已整理到 [本轮交接说明](docs/preview-reliability-handoff.md)。

**人工停止点 1 已完成：** PR #52 合并为 `6156165e0ea501093e3e31d5cffedd5118aeecfd`，预检资产另经 PR #53 合并为 `238bf013e57bcacb4b381093dd3761b760881a87`，两次 PR 的四项 GitHub 检查均通过。本轮仅补充部署记录，仍由用户手动提交新 PR。

## 5. 阶段 C：部署与模拟流程复核

- [x] 核对 PR #52 的实际合并提交及四项 GitHub 检查结果。
- [x] SSH 恢复后核对源码、镜像和实际服务版本；API/Web/Worker 已统一到 PR #53 合并提交，镜像标签为 `0.1.0-preview.1-238bf01`。
- [x] 准备只读预检、8 项离线测试及 [更新与回退操作单](docs/preview-rollout-plan.md)，不删除数据库卷或停止其他项目。
- [x] 预检资产已通过 PR #53 人工合并，四项 CI 成功，真实 Docker 模板验证已在该 PR 的 CI 通过。
- [x] 获得服务器变更确认后完成部署；停机后四类待处理计数均为零，无迁移差异，仅重建 API/Worker/Web，保留回退配置和旧镜像。
- [x] 用户确认本轮 HTTPS 登录、演示仓库、模拟任务审批及注销成功；未收集逐项截图或任务编号，不将其写为代理独立实测。
- [x] 用户在获得阿里云核查说明后回复“全部成功了”，确认安全组验收，包括 8080 公网限制和 SSH 可信来源。之前 HTTP 000 的技术探测本身仍不构成规则证明。
- [x] 更新个人预览文档的代码合并记录，并把旧线上版本标明为历史。
- [x] 已补充 [实际部署记录](docs/preview-rollout-result.md)，包含版本、健康检查和未完成边界，不将历史验收用于本轮。
- [x] 已补充用户本轮人工验收确认及其证据边界。

**人工停止点 2 已完成：用户确认模拟流程和安全组验收。** 不能据此标记“真实自动改代码通过”；完整五分钟演练与逐项证据仍可后续补充。

## 6. 阶段 D：一个真实任务，单独授权后执行

本阶段可能花钱，也会执行模型生成的代码。先确认条件，不能直接在公网服务上开启任意命令执行。

开始条件：

- [x] 核查现有入口：`plan` 只规划，`eval execute` 仅支持固定 `software/v1` 且要求 Grader；不能把公开 demo 当作私有 Fixture 直接套用。
- [x] 对原始公开 demo 做一次离线容器基线测试：非 root、源码只读、无网络，无模型调用。仅证明现有正常姓名测试通过，不证明目标需求已经实现。
- [x] 实现并离线验证独立任务入口：数据白名单、禁止自动重试的一次性调用计数、调用前费用预留、模型与测试环境分离、审批及证据保存均已接通。完成项只表示代码和模拟故障测试通过，不代表真实请求或账单验收完成。
- [x] 完成独立入口的离线预检部分：四文件白名单、128 KiB 限制、路径与文件变更检查、快照指纹，以及显式启用的无网络容器基线；总超时和按归属清理已有离线测试，不具备付费执行能力。
- [x] 完成 Fake Provider 下的持久化预算部分：请求前提交次数及费用预留、未知费用阻断、重启与并发保护、配置绑定、显式证据核对接口；只验证组件，不代表真实请求重试已接入或人民币上限已落实。
- [x] 完成离线补丁审查及审批摘要绑定：只允许替换两个 Go 文件，默认只看差异；显式批准后才把捕获的候选副本交给现有隔离沙箱。入口已通过 Fake 沙箱验证，本轮未运行真实候选代码，不代表付费请求、身份鉴权或持久化证据链完成。
- [x] 保存本地审查、批准和测试结果收据：执行前事务提交、同库一次性消费、异常/崩溃后阻断与结果摘要落盘已用 Fake 验证。只是收据，不是原始源码/提案归档、身份签名或跨库防重放，真实任务仍未执行。
- [x] 实现离线任务准备与核对入口：同一受控根目录内原子占用任务编号，任务正文、四文件原始快照和 Fake 预算保存在固定 `task.sqlite`；复制到其他位置、输入漂移和不完整初始化会阻断。仅覆盖输入归档与同根目录防误重建，不是跨根目录全局唯一性、防篡改系统或完整付费执行入口。
- [x] 接通同库提案与审批流程：人工导入的提案精确字节和初始收据在同一事务保存；审查与批准测试只读取归档候选，不能另传收据路径或覆盖旧提案；并发、进程中断及保存失败的阻断已用 Fake 验证。未调用模型或运行真实候选代码，不代表完整真实任务完成。
- [x] 接通离线 Fake 请求生命周期：批准绑定请求精确字节与预留金额；请求/批准/预算一起提交后才调用固定模拟函数，合法返回/提案/初始收据/零费用结算一起保存；未知结果保留预留并阻止重试，同任务人工导入与请求互斥。只有 Fake，不能宣称真实限费或数据发送授权已完成。
- [x] 完成 DeepSeek 协议与费用的离线模块：固定请求格式、显式输出上限/关闭思考、严格用量校验、价格有效期、整数人民币额度换算和保守预留估算；无效/截断提案保留可核对的费用估算。未实现真实 HTTPS、账单核验、付费授权或真实费用结算，不能据此开始付费。
- [x] 实现单次 HTTPS 内部组件并离线验证：固定 DeepSeek 地址、系统 TLS 验证、无重定向/重试、有限响应读取、凭据私有管道和子进程超时清理。模拟 HTTPS 与真实无网络子进程检查通过；没有真实握手或模型调用，也未接入授权/预算/回执持久化，不能据此直接付费运行。
- [x] 接通内部真实任务生命周期：旧 Fake 不转换；批准绑定计划，预算预留与请求开始在调用前一起提交；响应、费用估算和合法提案收据一起保存。超时、中断、并发和保存失败阻止重发，候选测试仍需另行批准。此项补齐上文 HTTPS 的内部接线，不代表身份鉴权、付费 CLI、实际账单或真实验收完成。
- [x] 提供离线真实任务操作 CLI：显式价格文件和人民币分额度、计划摘要与可选私有正文查看、批准参考文件、候选 Diff 和独立测试批准。分别显示请求状态与候选结果；错误不回显未知参数，过期价格可查看但不能批准。该轮已通过 PR #67 合并。
- [x] 增加受控凭据与单次发送入口：密钥仅从仓库和任务目录之外的受保护普通文件读取，不进入 argv、环境、数据库或普通输出；发送只接受已批准的精确计划，调用前提交次数与预算预留，任何未知结果均阻止重发。仅用假密钥和模拟网络验证，尚未真实调用。
- [x] 强制发送前保存沙箱基线收据：批准后的归档原始快照必须用已缓存镜像通过固定 Go 测试，收据绑定计划、快照、镜像摘要、超时和沙箱配置；缺失/篡改收据或当前镜像不可用时，在读取密钥前阻断。候选测试不能临时改镜像或超时。此项仅完成代码与模拟故障验证。
- [x] 选择一个独立演示任务：公开 demo 的 `Greet` 先去除姓名首尾空白；空值返回 `Hello, guest!`，否则返回 `Hello, <姓名>!`；补充普通姓名、中文姓名、首尾空白和空字符串测试。只允许修改 `greeting.go` 与 `greeting_test.go`。
- [x] 优先使用本地或专用隔离环境，不能访问业务密钥、私有评分程序和其他项目目录。本机 Docker Desktop 4.47.0 / Engine 28.4.0 可用；已缓存 `golang:1.22-alpine` 解析为固定镜像 `sha256:1699c10032ca2582ec89a24a1312d986a3f094aed3d5c1147b19880afe40e052`，公开归档快照的真实基线退出码为 0、未 OOM、不收集日志且无受管容器残留。只证明隔离环境和原有正常姓名测试可运行，不证明目标改动已完成。
- [x] 明确本次真实小任务走独立命令行入口；网页继续保持 Mock，命令行成功不能代替网页自动执行成功。
- [x] 固定允许的候选工具和停止机制：仅运行无网络、非 root、只读源码/根文件系统、限资源容器中的固定 `go test`；外层总超时 1～120 秒，只按任务标签清理本次容器，清理不确定时失败关闭。实际 Docker 权限、固定镜像解析、60 秒外层时限和成功清理已随上一项验收。
- [x] 用户明确确认模型服务、可发送的数据、调用次数和费用上限；本轮限定 `deepseek-flash`、公开 demo 任务及四文件、最多 128 KiB、恰好一次、禁止重试和人民币 3 元上限。
- [x] 密钥只通过受控方式配置，不发送到聊天或写入普通日志；未经单独同意不上传预览服务器。既有仓库外密钥文件已通过 `credential-check`，命令未保存或回显内容，也未上传服务器；该结果只验证本地文件条件，不是付费授权。

执行任务：

- [x] 只执行一个明确的小任务，记录代码、模型和配置版本。请求基于 `a2ad9b9`，使用 `deepseek-flash`、4,096 最大输出 token 和 60 秒超时；候选经过独立审查、测试和应用授权后写入公开 demo。
- [x] 展示任务输入、审批决定、真实代码改动、测试结果及费用结果。任务、授权、仓库 Diff 和隔离测试结果已有脱敏记录；本地估算约为人民币 0.00622 元，用户人工核对 DeepSeek 后确认实际扣费低于人民币 0.01 元且只有 1 次对应调用。具体账单核对日期未提供；不提交控制台截图、余额、请求明细或凭据。
- [x] 失败时区分模型输出、补丁应用、测试、权限和运行环境问题，停止自动增加付费重试。调用前的 Docker 未启动和启动初期基线失败均归类为本地运行环境问题，在模型调用数仍为 0 时修复；付费调用没有重试。
- [x] 原始记录留在受控位置，公开演示材料只放脱敏摘要。

完成标准：真实改动已经应用，相关测试确实执行并通过，供应商侧费用与调用次数已由用户人工核对。网页仍保持 Mock；该真实命令行案例不等于网页已启用真实执行。

**人工停止点 3 的候选应用已完成：** 用户针对精确候选快照另行授权只修改公开 demo 的两个允许文件。应用后的四文件快照哈希与已测试候选一致；没有再次调用模型、重复候选测试或部署服务器。

## 7. 阶段 E：面试演示与收尾

准备材料可以提前进行，不要求开启付费模型。以下材料和本地证据已经准备，用户也已逐项确认本轮演练结果；具体演练日期和页面显示版本未提供。

- [x] 准备 [5 分钟模拟演示稿](docs/preview-demo-walkthrough.md)：项目定位、任务填写、审批、事件记录与结果边界，并补充阶段 D 的真实代码改动与费用证据。
- [x] 准备固定输入的拒绝审批案例，说明审批应为“已拒绝”、任务应为“已取消”；本地 Mock 测试通过，用户随后确认线上任务为 `cancelled`、审批为 `rejected`。
- [x] 准备人工审批、断线恢复、请求去重的通俗口述与追问回答，注明不能保证所有外部操作恰好执行一次。
- [x] 材料明确区分模拟流程、单次真实案例和大规模评测；前两者已有各自证据，大规模评测仍未重跑。
- [x] 材料如实记录公开预览仍为 Mock，不宣称生产可用或效果优于现有基线。
- [x] 用户人工确认 HTTPS 登录、仓库选择、正向批准、拒绝后的任务 `cancelled` / 审批 `rejected`、刷新恢复和注销保护均成功；具体演练日期和页面显示版本未提供。此记录来自用户回报，不是代理独立浏览器实测。
- [x] 已用真实 Diff、隔离测试结果、本地费用估算及用户人工核对的供应商实际费用范围补充演示稿；估算值与实际扣费范围分别标注。
- [x] 处理演练暴露的表达问题：登录页直接说明公开预览是模拟流程；登录后的全局提示并列说明“网页当前可做、独立 CLI 真实案例、尚未启用”；新建任务页明确不会修改仓库、运行测试或产生模型费用，预算卡也注明配置上限不等于真实调用授权。相关前端测试、类型检查和仓库完整验证通过。源码尚未部署，不能把实现完成写成线上文案已验收。

本轮完成条件已经满足：三个问题完成修复与验证，预览流程经用户人工确认通过，一个真实小任务留下了可复核结果和明确的执行入口说明。公开网页仍为 Mock，正式批量评测与生产发布验收不在本轮完成范围内。

## 8. 留到最后的任务

以下暂不阻塞本轮个人演示，保持待办，不标记已完成：

- 正式批量模型评测和候选版本对比。
- 大规模并发、长期稳定性、完整恢复与容量测试。
- 正式生产镜像签名、安全复核、值班与发布签署。
- 新增多智能体角色、大规模知识库等非必要功能。

未来重新启动这些任务时，先使用 [3 阶段任务](3阶段任务.md) 查看重新审查后的压缩执行顺序，再回到 [正式剩余执行计划](FORGEFLOW_REMAINING_EXECUTION_PLAN.md) 和 [原路线图](FORGEFLOW_POST_IMPLEMENTATION_ROADMAP.md) 核对详细门禁，重新确认版本、授权、预算和验收条件。本计划不降低正式发布标准。

## 9. 每轮结束必须交代什么

- 完成了什么，修改了哪些文件。
- 实际检查了什么，结果和耗时如何。
- 哪些项目没做，为什么。
- 结果对应哪个提交或待提交工作区，服务器是否更新。
- 下一步需要用户做哪一个具体操作。

下一步：人工提交当前 `codex/user-data-lifecycle` 分支并创建 PR，不需要真实 Bucket、备份介质、部署、私有值或付费调用。Production 对象存储 PR #86 已合并为 `de5995d265d109eb99226b091f06819fa8a39e47`；本分支实现 owner-scoped ZIP 导出、敏感字段排除、密码二次验证、最后管理员保护、Session 撤销、异步用户删除、Artifact 清单与部分失败续跑、数据库级联、治理 actor 匿名化、管理员恢复 API、Web 自助入口及 Migration 6。该 PR 合并且 CI 成功后，下一项单独处理管理员 MFA/IAP 方案；真实对象版本和备份恢复验收仍留在“3阶段任务”的阶段二。

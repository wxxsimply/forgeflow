# 公开 demo 单次真实调用审查记录

记录日期：2026-09-17；代码基线：PR #72 合并后的 `a2ad9b9`。

## 授权与输入边界

仓库所有者明确授权使用 `deepseek-flash` 完成恰好一次真实付费调用，禁止自动重试，总费用上限为人民币 3 元。允许发送的数据只有公开 demo 的任务描述，以及 `README.md`、`go.mod`、`greeting.go`、`greeting_test.go`，合计不得超过 128 KiB；密钥、Private Grader、隐藏测试、原始 Evidence 和其他文件均不得发送。候选返回后只允许审查，执行候选代码需要再次单独批准。

实际计划载荷为 3,207 字节，输入快照只包含上述四个文件。请求配置为最大输出 4,096 token、60 秒超时、关闭思考模式，预算按短期官方价格快照保守预留。计划、授权摘要、沙箱收据和预算在发送前完成绑定。

## 调用结果

- 模型：`deepseek-flash`。
- 账本调用次数：1；没有重试。
- 状态：请求完成，返回的补丁结构通过白名单和绑定检查。
- 用量：输入 1,254 token、缓存输入 0 token、输出 464 token。
- 估算费用：0.000933 美元，按本次价格快照的汇率假设约为人民币 0.00622 元，低于 3 元上限。该数值是本地用量估算，不是 DeepSeek 账单。
- 响应 SHA-256：`38db1a1a5c8975aff337c80a3b1995c6ae0ed5a0cf42d2c4d4c7a9374905d7d7`。
- 候选快照 SHA-256：`7fd3a1e1282427f1099cffbf9f2905a099ac78f3a5da211834c7d653b654c5b0`。

调用前固定镜像基线最终通过：镜像摘要为 `sha256:1699c10032ca2582ec89a24a1312d986a3f094aed3d5c1147b19880afe40e052`，退出码 0、未 OOM、未收集日志。Docker 未启动及刚启动时的失败被归类为本地运行环境问题；两次都发生在模型调用数仍为 0 时。门禁通过后才执行唯一一次付费请求。

## 候选静态审查

候选只修改允许的两个文件：

- `greeting.go`：使用 `strings.TrimSpace` 去除姓名首尾空白；结果为空时返回 `Hello, guest!`，否则返回带清理后姓名的问候语。
- `greeting_test.go`：改为表驱动测试，覆盖普通姓名、中文姓名、首尾空白和空字符串。

静态审查未发现越界文件、与任务无关的改动或明显逻辑错误。

## 候选隔离测试

仓库所有者随后针对候选快照 `7fd3a1e1282427f1099cffbf9f2905a099ac78f3a5da211834c7d653b654c5b0` 单独授权恰好一次无网络隔离测试，禁止模型调用、自动重试或更换候选。执行前再次核对候选仍处于未执行状态、固定镜像摘要匹配且没有受管容器残留。

唯一一次测试结果：

- 状态：通过。
- 退出码：0。
- OOM：否。
- 固定镜像：`sha256:1699c10032ca2582ec89a24a1312d986a3f094aed3d5c1147b19880afe40e052`。
- 外层超时：60 秒。
- 网络：关闭。
- 日志：未收集。
- 候选证据状态：`completed`。

测试运行前后账本模型调用次数均为 1，当前测试命令没有越过模型传输边界，也没有产生新的模型费用。测试没有重试。

## 当前停止点

仓库所有者另行授权把已测试候选只应用到 `examples/preview-demo/greeting.go` 和 `examples/preview-demo/greeting_test.go`。应用后重新计算公开 demo 四文件快照，结果为 `7fd3a1e1282427f1099cffbf9f2905a099ac78f3a5da211834c7d653b654c5b0`，与已审查、已测试候选精确一致。没有修改其他业务源码、再次调用模型、重复候选测试或部署服务器。

候选原字节中的结构体字段对齐与 `gofmt` 规范相差一个空格。为保持仓库内容与已测试候选的字节级绑定，本轮保留候选原字节；现有 CI 的 Go 格式门禁只覆盖 `cmd`、`internal` 和 `migrations`。如后续决定格式化该文件，应把它作为新的、可审查的变更，不能继续声称快照与本候选精确一致。

原始请求、完整响应、批准记录和任务数据库继续保存在仓库外的受控位置，未加入 Git。

上一轮记录文档完成后运行了仓库自身检查：preview demo Python 快速集合共 226 项，224 项通过、2 项因平台不适用而跳过，耗时 23.077 秒；固定版本 `govulncheck` 未发现代码可达漏洞；`scripts/verify.ps1` 退出码为 0。这些结果验证当前仓库和操作入口；上节的固定容器结果才是候选代码测试证据。

加入候选测试脱敏记录后再次运行相同提交前检查：226 项快速集合中 224 项通过、2 项平台跳过，耗时 27.384 秒；固定版本 `govulncheck` 仍未发现代码可达漏洞；仓库级 `scripts/verify.ps1` 退出码仍为 0。

当前真实小任务的改动应用和隔离测试已经完成。供应商账单仍未核验，网页继续使用 Mock，服务器也未部署本次改动；这些边界不能由命令行案例代替。

应用候选后的提交前检查中，固定版本 `govulncheck` 未发现代码可达漏洞，仓库级 `scripts/verify.ps1` 完整通过，包括 Go 测试与构建、迁移检查、23 项前端测试和 Vite 构建。脚本层快速集合在本机 Windows 运行 226 项，其中 223 项通过、2 项平台跳过，`test_real_hung_child_is_reaped_within_supervised_deadline` 因耗时约 30 秒而未满足 10 秒限制；单项复核结果相同。该 HTTPS 子进程模块未被本轮修改，PR #74 的 Linux CI 中同一集合通过，但新 PR 仍必须等待自己的 GitHub CI，不能把历史 CI 当作本次通过。

候选应用随后经 PR #75 合并为 `4403323`。本次 PR 的 Go verification、Web verification、PostgreSQL integration 和 deployment-assets `validate` 四项检查全部成功；Linux `validate` 实际运行了完整 preview demo 脚本集合，因此本机 Windows 的子进程回收时限失败没有在合并门禁中复现。该结论只解决平台测试证据，不改变“费用仍是估算、服务器未部署、网页仍为 Mock”的边界。

## 剩余人工证据

- 在 DeepSeek 控制台核对本次调用对应的实际扣费；只记录金额和核对日期，不提交账户截图、余额、请求明细、密钥或其他任务数据。
- 按 [5 分钟演示稿](preview-demo-walkthrough.md) 实际排练网页 Mock 流程与独立真实案例讲解，并填写第 6 节；不能用此前笼统的“全部成功”代替逐项演练记录。

# 阶段 C：个人预览实际部署记录

记录日期：2026-09-14。范围是个人预览，不代表生产发布或真实自动改代码验收。

## 最新人工确认

用户在本轮网页验收清单及阿里云安全组核查说明后回复“全部成功了”。据此记录阶段 C 的网页模拟流程和安全组人工验收通过。确认来源是当前会话中的用户反馈；未取得逐项截图、任务编号或云规则导出，代理本轮未重新登录或修改服务器。

下文第 3～4 节保留部署当时的检查边界和操作说明，其中“待确认”是历史状态，现由上述用户确认补充。此前 HTTP 000 不被重新解释为端口关闭的技术证据。此验收不涵盖真实模型改代码、付费调用或生产发布。

## 1. 已完成

- PR #53 已合并为 `238bf013e57bcacb4b381093dd3761b760881a87`，Go、Web、PostgreSQL、validate 四项 CI 均成功。
- 用户人工准备专用演示根后，API 实际读到演示仓库提交 `920a1a59eb914f7baa1d5c0d8b4caec3c2db8d56`，分支为 `main`。
- 在用户明确授权下完成更新，API、Worker、Web 使用唯一标签 `0.1.0-preview.1-238bf01`，实际运行提交均与 PR #53 一致。
- 只机械更新私有环境文件中的版本、完整提交编号和仓库路径三个字段，文件权限保持 `600`；原配置和旧镜像留在服务器私有回退记录中，不将环境文件加入 Git。
- API/Worker 仍以 `10001:10001` 运行；API 只读、Worker 可写挂载专用演示根。原仓库目录及权限保持不变。
- 保持 `planning + mock`，真实 Docker 执行关闭；没有上传模型密钥、调用付费模型或执行真实改代码任务。

## 2. 执行与检查证据摘要

- 切换前核对三个候选镜像标识、回退镜像和原环境备份；目标与旧线上后端的 `migrations` 无差异，无需执行数据库迁移。
- 旧服务运行时完成镜像构建；前端构建含类型检查、23 项 Vitest 测试和生产打包，均通过。此构建发生在前一轮已授权工作中，本轮未重复构建或跑全量评测。
- 本轮部署前只读预检 23 项通过。停止 API/Worker 后，再次查询旧 pending、未结束任务、待处理队列、未发布 Outbox，四项均为零，没有删除或取消记录。
- 使用基础 Compose 和 HTTPS overlay，执行 `up -d --no-deps --no-build api worker web`，只重建三个应用服务。
- 部署后只读预检 28 项通过，五个 ForgeFlow 服务健康；核对源码、实际 API/Web 响应、Worker 镜像版本、目录读取、端口与证书挂载。
- 外部 HTTPS 检查未跳过证书验证：`/healthz` 为 `status=ok`，`/release.json` 与 API 版本一致，`/login` 返回 HTTP 200。
- Caddy/PostgreSQL 的容器 ID、镜像 ID 和启动时间与部署前一致；五个 TTS 容器 ID 与镜像名称一致，本轮未执行其重启命令。
- 全程没有 `down`、卷删除、镜像清理、密码重置、自动 Git 提交或 PR 操作。

预检返回的 `deployed:false` 表示该只读工具本身不执行部署，不代表外部部署没有发生；本次部署事实由 Compose 执行结果及更新后的运行版本共同确认。

## 3. 网络检查的边界

容器端口检查确认：443 对外发布，8080 只绑定宿主机回环地址，API、Worker、Web 和数据库不直接发布宿主机端口。

本轮从外部请求 8080 得到 HTTP 000 / `Empty reply from server`，未取得 ForgeFlow JSON。该结果不能单独证明 TCP 8080 被云安全组拒绝。SSH 连接成功也不证明其来源仅限可信 IP。云安全组的 8080 拒绝规则及 SSH 来源限制仍需用户在控制台复核；本轮没有修改这些规则。

## 4. 接下来由用户确认

打开 `https://39.102.136.31/login`，确认没有证书警告，使用现有账号登录，不要发送密码到聊天。

在登记仓库处填写：

- 名称：`demo`（如已登记相同路径，直接选用现有仓库）。
- 本地绝对路径：`/repositories/demo`，这是容器内路径，不是 Windows 或宿主机路径。
- 默认基线：`main`。

创建一个模拟任务，例如“检查 greeting.go，提出空名字的处理建议”；查看任务、审批和结果页面，最后注销。记录实际是否成功及报错请求 ID，不将密码、Cookie 或完整执行记录放入公开 PR。

目前只确认 API 能读仓库，还没有用用户账号完成网页登记、模拟任务、审批和注销。本轮浏览器确认必须单独记录，不能沿用旧版本结果。模拟流程成功也不能标记成真实模型修改源码成功。

## 5. 本轮交接与后续边界

本轮本地分支为 `codex/preview-rollout-record`，仅修改文档。Git 提交、推送、PR 创建和合并由用户人工操作；命令直接在聊天中提供。记录 PR 合并不需要再次部署应用镜像。

阶段 C 待浏览器确认和云安全组复核；阶段 D 待选择独立任务、隔离环境、发送范围与费用上限的本轮授权。正式批量 Eval、长时间压力测试与生产发布门禁继续延后，不标记完成。

# 阶段 D：独立真实任务准备记录

记录日期：2026-09-14；代码核查基线为 PR #55 合并提交 `fc43f59abc2d730b59f40751c70ec828e87e739e`。本轮不调用模型、不改线上服务，不把准备工作当成真实任务完成。

## 最新实现：离线预检入口

PR #56 已合并为 `ba2231d8b3672897419ca518063c83692a444174`。在其基础上新增 [预检工具](../scripts/preview_demo_preflight.py) 与 [离线测试](../scripts/test_preview_demo_preflight.py)，仅完成独立执行入口的输入检查和基线沙箱部分，仍无付费执行功能。

默认运行只读取公开模板，返回文件名、大小和 SHA-256 指纹，不读取密钥、不启动 Docker、不发送网络请求：

```powershell
python -B scripts/preview_demo_preflight.py
```

只有显式指定 `--sandbox` 才会执行本地容器基线测试：

```powershell
python -B scripts/preview_demo_preflight.py --sandbox --timeout-seconds 90
```

要求本机已有受信任的 Go 镜像，默认 `golang:1.22-alpine`。工具先解析本地不可变 image ID，禁止自动拉取，不能使用未经审查的镜像。`--source` 只能指向你控制的、执行期间不被并发修改的平铺四文件模板目录；目录含 `.git` 或其他文件会被拒绝，所以不要指向完整 ForgeFlow 仓库或已初始化的线上 demo。

已实现的边界：

- 只接受 `README.md`、`go.mod`、`greeting.go`、`greeting_test.go`，总计不超过 128 KiB；拒绝未知文件、子目录、`..`、符号链接、Windows reparse point、硬链接、二进制和无效 UTF-8。
- 核对固定模块声明和 Go 版本，禁止添加依赖声明。检查读取前后的文件标识与时间，减少读取过程中内容变化的风险；这不是针对恶意并发文件系统写入者的完整隔离保证。
- 将捕获的字节复制到一次性目录后才挂载容器，源码只读；不挂载原仓库、Docker socket、凭据或宿主机缓存。
- 无网络、非 root、只读根、禁止提权、CPU/内存/进程数限制，只有用于 Go 测试的临时 `/tmp` 可写可执行。缓存及工具链下载关闭。
- Docker 日志驱动设为 `none`，不收集测试 stdout/stderr，避免不受限的原始输出；报告只包含退出码、OOM 状态和镜像 ID，失败细节需要另行受控诊断。
- 外层总截止时间覆盖镜像检查、容器创建、启动、编译与测试；默认 90 秒，最大 120 秒，另预留最多约 10 秒验证归属并清理。只操作本次随机名称且 owner label 匹配的容器，不批量删除。
- 创建超时也会尝试核对清理；若无法确认清理，返回 `cleanup_unconfirmed` 和本次容器名，不能算成功。不要自行运行全局容器或卷清理。

报告的 `checksPassed:true` 仅代表本次文件检查或原始基线测试通过。`readyForPaidExecution` 和 `paidExecutionEnabled` 始终为 `false`；指纹不是审批签名，文件白名单也不是密钥扫描器。

本轮检查：16 项离线单元测试通过（约 0.36 秒），已接入部署资产 CI；默认预检通过，当前模板为 1,081 字节。真实 Docker 基线通过，总耗时 35.39 秒，未 OOM，原始源码未改动。本轮没有测试新需求、没有费用、没有修改线上服务。后续实际源码换行或内容变化时，字节数和指纹可能不同，应重新核对，不能复用旧指纹。

另用 1 秒外层时限执行真实 Docker 负向检查，按预期返回 `checksPassed:false` / `docker_command_timeout`，无本工具标记的容器残留。这个结果是预期失败路径，不是业务测试失败；清理检查通过后才结束本轮。

下文保留早期核查背景；“入口未实现”现特指付费请求、重试计数、费用预留、补丁审批和真实任务证据链尚未接入，而不是没有任何 CLI 工具。

## 1. 已确认与未授权的边界

用户已回复“全部成功了”，用于阶段 C 的网页模拟流程和安全组人工验收。此确认发生在新的付费建议之前，不是对阶段 D 的付费授权。

随后提出的“DeepSeek 最多 3 次、总费用最多人民币 1 元、只发送独立 demo 数据”的建议尚未得到明确确认。本轮未读取密钥、未向 DeepSeek 发送数据。历史私有 Eval 授权不扩展到本次任务。

## 2. 待用户确认的任务规格

使用 [公开 demo 模板](../examples/preview-demo/README.md) 的独立副本，不使用线上服务的挂载仓库，不复制 ForgeFlow Git 历史或私有评测资产。

目标：修改 `Greet(name string) string`，先去掉姓名首尾空白；结果为空时返回 `Hello, guest!`，否则返回 `Hello, <姓名>!`。只允许改 `greeting.go` 和 `greeting_test.go`，不改变函数签名、模块依赖或其他文件。

建议验收输入输出：

| 输入 | 预期返回 |
| --- | --- |
| `ForgeFlow` | `Hello, ForgeFlow!` |
| `小明` | `Hello, 小明!` |
| 两边有空格的 `Ada` | `Hello, Ada!` |
| 空字符串 | `Hello, guest!` |
| 仅空格、制表符和换行 | `Hello, guest!` |
| `Ada Lovelace` | `Hello, Ada Lovelace!` |

这只是待执行任务规格，没有提前修改模板实现或伪造模型补丁。原始模板目前只实现直接拼接，现有测试只覆盖普通姓名。

## 3. 现有入口核查结果

- [CLI](../cmd/forgeflow/main.go) 的 `plan`/`approve` 使用规划 Service，不能仅靠切换模型就得到完整开发执行。
- `eval execute` 只接受 `software/v1`，要求 Fixture 与 Grader 仓库。`--limit` 限制新增案例数，不等于 HTTP 请求次数上限，不能把它写成“最多调用三次”。
- [Eval 费用控制](../internal/evalexec/budget.go) 已有调用前预留及累计费用机制，但使用美元，且没有自动接入独立 demo 命令。人民币上限、价格有效期、重试计数和未知费用处理仍须为新入口核对。
- [Grader 执行](../internal/evalexec/grader.go) 使用宿主机 `go run`；Git worktree 本身不是进程隔离，不能因为用了 worktree 就宣称生成代码完全隔离。
- [开发流程测试](../internal/application/development_test.go) 使用固定 Fake 补丁及测试 Runner，不能当作已经接通真实模型的命令行入口。

结论：不直接复制旧的付费 Eval 命令，也不在公网部署中开启 Docker 执行。先补齐专用入口，并用 Fake Provider 验证预算、越界、异常和停止路径，才进入真实调用。

## 4. 本轮免费隔离基线检查

本机 Docker Engine 可连接，版本为 `28.4.0`。使用已缓存的 `golang:1.22-alpine`，运行前解析本地不可变 image ID，`--pull never`，没有下载镜像。只将 `examples/preview-demo` 只读挂载至 `/workspace`。

隔离条件：无网络、用户 `10001:10001`、只读根文件系统、去掉所有 Linux capabilities、禁止提权、最多 128 个进程、384 MiB 内存、1 CPU。没有挂载用户目录、凭据、其他仓库或 Docker socket。

Go 缓存置于 128 MiB 临时 `/tmp`，显式设置 `rw,exec,nosuid,nodev`；禁用模块代理与自动工具链下载。运行 `go test -mod=readonly -count=1 -timeout 30s ./...`。

实际结果：

- 第一次未显式设置 tmpfs 的 `exec`，测试二进制在 `/tmp` 执行时报 `permission denied`，本次失败保留，不计为通过，总耗时 29.38 秒。
- 仅为一次性容器临时目录明确 `exec` 后重试，基线通过：测试报告 0.004 秒，命令总耗时 28.19 秒。源码挂载始终只读，容器结束自动移除，没有修改线上权限。
- 两次合计约 58 秒，均无模型调用。通过的是原始正常姓名测试，不是空名字等新需求验收，也不是生成代码安全测试。

注意：`go test -timeout` 限制测试运行阶段，并非整个编译和容器生命周期的截止时间。真实任务入口还必须实现外层总超时及指定容器清理，不能仅依赖这个参数。

## 5. 专用入口必须具备的保护

- 默认仅预检；显式授权后才能发送请求。保留唯一任务编号和基线 SHA，不能覆盖之前的证据或重置预算重试。
- 发送内容只从四个公开模板文件及任务描述构造，总源码快照不超过 128 KiB；排除 `.git`、环境文件、凭据和任何其他项目数据。拒绝符号链接、异常路径和未知文件。
- 模型调用进程与执行测试的容器分开：密钥不进入测试容器。补丁必须通过路径与大小检查、人工审批后，才能应用到独立工作副本。
- 把底层重试也计入用户确认的请求总数；请求前预留保守最高费用。超时或费用不明时保留预留并停止，不能当作免费请求继续调用。
- 执行前核对官方价格、模型、价格有效期和人民币预算的保守转换方案；没有可靠上限就不启动。文档中的建议额度不代表已经授权。
- 仅执行固定 Go 测试命令，使用无网络、非 root、限资源的隔离容器；限制整体耗时、输出大小，能停止且只清理本次任务容器。
- 保存真实补丁、测试退出码、输入版本、请求次数与实际用量；公开 PR 只放脱敏摘要。确认原始模板和线上仓库不变。

## 6. 当前停止点

本轮停止在离线预检工具的人工 PR 提交点。后续可继续实现 Fake Provider 下的调用次数、费用预留和补丁审批测试；实际发送数据仍需明确的任务范围与付费授权，即使收到授权，保护检查不通过也不能发起模型请求。

命令行真实案例通过后，只能标记“命令行案例通过”；网页真实执行、完整演练与大规模模型效果仍是独立待办。

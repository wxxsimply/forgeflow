# ForgeFlow 3 阶段任务

> 重新审查日期：2026-09-18
> 审查基线：`38a10120e0e365c984fe72885c85aa4b6594b4e6`（PR #84 合并后的 `main`）
> 执行状态更新：2026-09-20；当前对象存储实现分支 `codex/production-object-store` 基于 PR #85 合并后的 `main`（`a5626d135d4e567fca89680d2f3242560c9e54f1`）。
> 总体结论：个人预览、面试演示、主体工程和阶段 0～3 已完成；按原路线图的 `v1.0.0` / Production 完成定义，项目尚未完成。
> 文档定位：本文把未完成工作压缩为三个串行阶段；`FORGEFLOW_POST_IMPLEMENTATION_ROADMAP.md`、`FORGEFLOW_REMAINING_EXECUTION_PLAN.md` 和各阶段 Runbook 仍是详细验收标准。

## 1. 重新审查结论

| 范围 | 当前状态 | 结论依据 |
|---|---|---|
| 主体代码与持续集成 | 工程基线可用 | Go、Web、PostgreSQL 和 deployment-assets 四项 GitHub 必需检查在 PR #84 全部成功；本地快速验证入口和阶段 5～9 静态契约已建立 |
| 个人公开预览与面试演示 | 本轮目标完成；部署版本非最新 | 用户已确认 HTTPS 登录、仓库选择、批准/拒绝、刷新恢复、注销保护和安全组；单次真实 CLI 案例有隔离测试与费用/调用次数人工核对；后续能力边界文案尚未部署 |
| 阶段 0～3 | 已完成 | Git/GitHub 基线、30 个 Fixture、隔离 Private Grader、三基线 Eval 和人工签署均已有记录 |
| 阶段 4～8 | 仅工程准备完成 | Prompt/模型治理、镜像、Staging、运维安全和 Production 文档/脚本齐备，但真实环境证据未完成，状态仍是“待集中验收” |
| 阶段 9 | 进行中且阻断 | 私有 Freeze 计划未通过；新的候选 smoke、正式 Eval、镜像、真实 Staging、恢复/安全/负载和最终发布均未完成 |
| `v1.0.0` / Production Ready | 未完成 | Go/No-Go 未签署，`P8-001`～`P8-007` 未关闭，未创建签名 Tag 或 GitHub Release |

因此，项目只能描述为“主体工程和个人预览完成，正式发布验收进行中”，不能描述为 Production Ready，也不能宣称 `v1.0.0` 已完成。

## 2. 当前未完成的关键事实

### 2.1 Freeze 尚未通过

私有计划 `.forgeflow/acceptance/1.0.0/plan.json` 已被 Git 正确忽略，但当前预检失败：

- 仍有 9 个未解析的私有字段：Eval 数据范围记录、Eval 预算记录、USD 费用上限、Release 审批记录、Staging HTTPS origin、签名身份、OIDC issuer、值班记录和独立安全复核记录；
- 候选字段虽然是完整 40 位 SHA，但不等于当前 `main`；
- 任何仓库 PR 合并都会改变候选 HEAD，因此只能在本计划 PR 合并后的干净 `main` 上重新生成 Freeze 计划。

不得使用示例值、短 SHA、旧分支或口头授权绕过此门禁。

### 2.2 候选质量仍未得到新证据

2026-09-09 的 `developer/v1` / `developer/v4` 两个 Observation 均在 `patch_check` 因损坏 unified diff 失败，显式测试和隐藏测试没有运行。安全规范化修复已经合并，但尚未由新的冻结候选 smoke 证明有效，因此不能启动正式 180 Observation Eval，也不能批准 Promotion。

### 2.3 Production 必需能力仍有真实缺口

- `P8-001`：S3/S3-compatible 适配器、KMS/tenant key/完整性/删除、逐 Run 迁移与失败补偿代码基础已准备；真实 Bucket、IAM、版本/生命周期、访问审计、迁移和恢复证据尚未完成，因此风险仍为 `Open`。
- `P8-002`：独立控制面、执行面、Sandbox daemon、节点和网络策略尚未在真实 Production 环境部署。
- `P8-003`：应用没有内建管理员 MFA，也没有已验收的上游 IAP 强制 MFA。
- `P8-004`：外部 append-only 审计与受控 Trace 后端尚未接入。
- `P8-005`：Provider、Region、数据驻留、子处理者和用户告知尚未最终批准。
- `P8-006`：负载、容量、RPO/RTO、故障切换和恢复尚未实测。
- `P8-007`：独立安全评审人、Primary/Secondary 值班和 Release Approver 尚未以私有记录固化。
- 用户自助导出端点和完整用户级联删除编排仍未实现；隐私政策和服务条款仍是草案。

### 2.4 发布环境证据仍为空

五类发布镜像尚未完成真实构建、SBOM、provenance、扫描、签名、上传和 digest 拉取验证；公网 HTTPS Staging、治理演练、E2E、告警投递、备份恢复、安全、回滚、Demo、负载和稳定性验收尚未形成真实证据。

### 2.5 非 Production 阻断但尚未收口

- 个人预览服务器最后有记录的部署版本为 `238bf013e57bcacb4b381093dd3761b760881a87`；后续能力边界文案已合并但未部署。应明确保留固定演示快照，或在独立授权、回退记录齐备后另行更新，不得把个人预览验收当作 Production 证据。
- 部分历史审计文档保留了当时的状态，个别“待合并/待验证”描述已过时。本文是当前复核结论；最终发布前仍须按真实证据更新完成度审计、原路线图和各阶段状态，同时保留历史记录。

## 3. 三阶段共同规则

1. 三个阶段严格串行；前一阶段未通过时，后续阶段保持 `blocked`，不得用 `skipped` 绕过。
2. 所有候选证据必须绑定同一个完整 Git SHA、Fixture commit、Private Grader commit、Prompt、模型、Reasoning、Policy、Tool、Migration 和费用窗口。
3. 原始模型输出、隐藏测试、Private Grader、服务器日志、数据库 dump、凭据和私有审批记录只保存在 Git 忽略目录或批准的私有存储。
4. 任何付费模型调用、Registry 写入、部署、告警投递、恢复、Promotion/rollback、Tag 和 Release 都需要独立的明确人工授权。
5. 仓库修改由自动化准备和验证；提交、推送、创建 PR、合并、签名 Tag 和 GitHub Release 始终由用户手动执行。
6. 每个门禁只允许 `pending`、`passed`、`failed` 或 `blocked`，并记录 UTC、角色、输入摘要、退出码和 Evidence SHA-256。
7. 候选 SHA、Prompt、Fixture、Grader、预算、数据范围或环境发生变化时，使受影响证据失效并从对应门禁重新开始。

## 4. 阶段一：补齐发布代码、冻结候选并完成正式 Eval

> 对应门禁：`freeze` → `candidate-smoke` → `formal-eval`
> 当前状态：`in_progress`，先完成冻结前仓库能力收口；Freeze 门禁保持 `blocked`。

### 目标

先完成所有会改变候选 SHA 的 Production 必需代码与部署契约，再得到一个可追溯、预算受控、数据范围获批的冻结候选；证明补丁协议修复有效，完成 `developer/v1` 与 `developer/v4` 的正式可比 Eval，并形成明确的候选批准或否决结论。

### 任务

#### 冻结前仓库能力收口

- [x] 实现并测试 Production 对象存储适配器，覆盖加密、租户隔离、完整性、删除、逐 Run 迁移和失败恢复，为关闭 `P8-001` 提供代码基础；真实环境关闭条件仍在阶段二验收。
- [ ] 实现 owner-scoped 用户数据导出和完整用户级联删除编排，覆盖幂等、租户边界、部分失败恢复、备份删除清单和审计。
- [ ] 选定管理员 MFA 方案；若使用应用内 MFA 则完成实现与独立测试，若使用 IAP 则固定可信身份头、绕过防护和部署契约，为关闭 `P8-003` 提供工程基础。
- [ ] 实现外部 append-only 审计和受控 Trace 后端适配，覆盖失败关闭、脱敏、查询和恢复，为关闭 `P8-004` 提供代码基础。
- [ ] 把批准的 Provider、Region、数据驻留、子处理者和用户告知要求固化为可验证配置门禁，为关闭 `P8-005` 提供工程基础。
- [ ] 完成独立控制面/执行面、Sandbox daemon、工作负载身份、网络策略和 Secret Manager 的部署契约，为关闭 `P8-002` 提供工程基础。
- [ ] 上述每项分别经过必要的人工 PR、四项必需 CI 和安全复核；最后一次代码 PR 合并前不得冻结候选。
- [ ] 明确个人预览是保留固定演示快照，还是另行更新到新的批准版本；若更新，使用独立部署授权和回退记录，不把预览验收当作 Production 证据。

#### Freeze、smoke 与正式 Eval

- [ ] 全部冻结前能力 PR 合并后更新到干净 `main`，确认 HEAD 为完整 40 位 SHA，已跟踪工作区干净。
- [ ] 在仓库外准备 9 个私有 Freeze 输入；核对 Fixture、Private Grader、Prompt、模型、Reasoning、Policy、Tool、Migration 和 Registry 是否仍采用当前批准值。
- [ ] 保留现有部分填写计划的私有备份，使用 `initialize-stage-9-acceptance-plan.ps1` 重新生成计划，不手工覆盖旧文件。
- [ ] 运行填充计划预检并通过；确认计划位于 Git 忽略目录、无占位符、候选 SHA 等于当前 HEAD。
- [ ] 重新确认当次数据发送范围、官方价格有效窗口、共享 USD 硬上限和恰好一次的新 Campaign 授权。
- [ ] 运行 1 Case × 2 Prompt 候选 smoke；确认两侧均通过 JSON、变更集、`git apply --check`、补丁应用、显式测试和隐藏测试，Fixture 前后不变。
- [ ] smoke 通过后，另行批准并完成 30 Fixture × 3 模式 × 2 Prompt，共 180 Observation 的正式 Eval。
- [ ] 保留全部失败、拒绝、超时和人工介入样本；核对完成率、隐藏测试通过率、回归率、人工介入率、总费用和 P95 延迟。
- [ ] 由人工评审正式 Eval：批准候选、保持 `developer/v1`，或要求修改；不得由自动 Gate 代签。
- [ ] 提交仅含聚合指标、版本摘要和 Evidence 哈希的脱敏公开报告；原始 Evidence 不进入 Git。

### 退出条件

- Freeze、candidate smoke 和 formal Eval 三个门禁均为 `passed`；
- 所有预期会进入 `v1.0.0` 的仓库代码与部署契约已经合并，离线/集成测试通过，冻结后没有计划内代码变更；
- 正式 Eval 的配置可比、180 个 Observation 均有终态、费用未超上限；
- 候选结论由授权人明确签署；若候选不通过，则记录 `NO-GO` 并回到候选修复，不进入阶段二。

### 必须停止的情况

出现候选 SHA 漂移、占位符、价格过期、预算不足、数据授权失效、补丁/Worktree/Grader 基础设施错误、未知调用结果或 Fixture 污染时立即停止。冻结后的任何仓库修改都使候选 smoke 和正式 Eval 证据失效；必须单独建立 PR，合并后从 Freeze 重新开始。

## 5. 阶段二：完成不可变镜像与真实 Staging

> 对应门禁：`image-supply-chain` → `staging-deploy` → `governance-drill` → `staging-e2e`
> 当前状态：`pending`，仅在阶段一批准候选后开始。

### 目标

不再改变阶段一批准的仓库内容，以同一 Git SHA 构建不可变供应链资产，并在真实 HTTPS Staging 验证 Production 控制、治理和业务全链路。

### 任务

#### Production 控制真实验收

- [ ] 在真实对象存储验证加密、租户隔离、完整性、删除、迁移和失败恢复，关闭 `P8-001`。
- [ ] 在真实身份入口强制管理员 MFA，验证可信身份传递、绕过拒绝和 break-glass 流程，关闭 `P8-003`。
- [ ] 在外部 append-only 审计和受控 Trace 后端验证删除保护、访问追踪、脱敏、查询和恢复，关闭 `P8-004`。
- [ ] 批准并验证 Provider、Region、数据驻留、子处理者、保留策略和用户告知/接受记录，关闭 `P8-005`。
- [ ] 部署独立控制面/执行面、Sandbox daemon、工作负载身份、网络策略和 Secret Manager，为关闭 `P8-002` 提供真实环境。
- [ ] 在 Staging 验证用户导出/删除的租户边界、幂等、部分失败恢复、下载过期和备份删除清单。

#### 镜像供应链

- [ ] 从批准 commit 构建 API、Worker、Web、Caddy、Sandbox 五类 `linux/amd64` 镜像。
- [ ] 生成并验证 SBOM 与 provenance；运行漏洞扫描，保持 Critical=0、High=0，或仅对符合规则的不可达 High 建立限时风险接受。
- [ ] 使用批准的 OIDC 身份签名；由发布负责人手动登录 Registry 并上传。
- [ ] 记录五个 digest，在干净环境按 digest 拉取并验证，生成全部 digest-only 的 Release manifest。

#### 真实 Staging 与治理

- [ ] 准备真实 Staging 主机/云环境、DNS、可信 TLS、Registry 只读凭据、Secret、备份目标和访问控制。
- [ ] 运行 Staging preflight，按 digest 部署；确认只有 HTTPS 对公网开放，数据库、Worker、监控和 OTLP 不暴露公网。
- [ ] 验证 Migration 5、Bootstrap Secret 删除、API/Worker/Web/Prompt/Git SHA/manifest 一致。
- [ ] 在 Worker drained 的前提下执行 Eval 导入、Promotion、Readiness 不匹配拒绝、旧 Run 绑定保持、rollback 和恢复候选；所有治理变更分别授权。
- [ ] 完成 HTTPS 登录、Session、RBAC、创建 Run、计划审批、Patch 审批、Sandbox 测试、重试、Artifact/报告下载和 Fixture 不变性 E2E。
- [ ] 验证真实执行面无宿主 Docker Socket、Sandbox 非 root/只读/无网络/零 capabilities，并完成正负网络测试，关闭 `P8-002`。

### 退出条件

- 五镜像供应链证据和 Release manifest 完整，镜像均固定到 digest；
- Staging deploy、governance drill、staging E2E 四个门禁均为 `passed`；
- `P8-001`～`P8-005` 按各自关闭条件处理；`P8-002` 属于 Critical，必须 `Verified Closed`，不可风险接受；其他 High 只有符合原安全政策时才能限时接受；
- 用户导出/删除、MFA、append-only 审计和执行面隔离在真实环境得到证据。

### 必须停止的情况

任何镜像签名/digest 不一致、Critical/未接受 High、Secret 泄漏、跨租户、Sandbox 逃逸、MFA 绕过、审计可删除、Staging SHA 漂移或 E2E 失败都立即停止，不进入阶段三。若需要修改仓库，返回阶段一，合并修复后重新 Freeze、smoke 和正式 Eval。

## 6. 阶段三：运维/恢复/负载验收并发布 v1.0.0

> 对应门禁：`operations-security` → `load-recovery` → `final-go-no-go` → `github-release`
> 当前状态：`pending`，仅在阶段二全部通过后开始。

### 目标

用真实、可重复、可追溯的运行证据证明服务可值守、可恢复、可回滚并满足容量目标，完成独立安全签署和最终人工发布。

### 任务

#### 运维、安全与恢复

- [ ] 向主/备值班渠道真实投递并解决 9 类合成告警，核对脱敏和 Runbook 跳转。
- [ ] 创建真实备份、checksum 和异地副本；在隔离数据库恢复并执行登录→Run→审批→报告验证。
- [ ] 执行路径穿越、符号链接、Shell 元字符、未知命令、Secret、RBAC、mTLS、Sandbox、网络和日志脱敏安全演练。
- [ ] 使用旧 digest manifest 完成应用回滚，不执行 Down Migration；验证 Schema 兼容、Readiness、Prompt/model 和审计记录。
- [ ] 由另一名 Operator 按文档重复 3～5 分钟 Demo，确认 Fixture checkout 不变。

#### 负载、容量与故障恢复

- [ ] 先批准并发、持续时间、数据范围、最大费用和中止阈值，再运行负载与稳定性测试。
- [ ] 测量 API、Worker、Queue、PostgreSQL、Object Store 和 Sandbox 的吞吐、错误率、P50/P95/P99 与资源水位。
- [ ] 执行 Worker/API/数据库/对象存储故障、队列积压和恢复，记录实际 RPO/RTO。
- [ ] 用实测值回填并批准 SLO、容量、错误预算、RPO/RTO，关闭 `P8-006`。

#### 最终治理与发布

- [ ] 在私有系统固化独立 Security Reviewer、Primary/Secondary on-call、Platform/Data/Service Owner 和 Release Approver，关闭 `P8-007`。
- [ ] 完成独立安全评审，确认 Open Critical=0、未接受 High=0，所有接受项含 Owner、补偿控制和到期日。
- [ ] 批准隐私政策、服务条款、Provider/Region/子处理者清单及数据导出/删除运行流程。
- [ ] 汇总正式 Eval、镜像、Staging、治理、E2E、告警、安全、恢复、回滚、负载和 Demo 的脱敏证据。
- [ ] 用最终真实证据更新 `docs/completion-audit.md`、原路线图和各阶段审计状态，清理过时的“待合并/待验证”描述，同时保留历史记录。
- [ ] 将阶段 4～8 从“待集中验收”更新为“已完成”，由全部必需角色签署唯一的最终 `GO`；否则保持 `NO-GO`。
- [ ] `GO` 后由用户手动创建并验证签名 `v1.0.0` Tag，手动创建 GitHub Release，上传经脱敏审核的 Release Notes、SBOM 和汇总资产。
- [ ] 发布后执行健康检查、监控观察和回滚判定，记录最终结论。

### 退出条件

- operations/security、load/recovery、final Go/No-Go 和 GitHub Release 四个门禁均为 `passed`；
- `P8-001`～`P8-007` 全部按原关闭条件处理；Critical 必须 `Verified Closed`，High 只有原安全政策明确允许时才能限时风险接受；
- 阶段 4～8 均有真实证据并标记为“已完成”；
- 签名 Tag、GitHub Release、批准 commit、Release manifest 和 Staging/Production 镜像 digest 完全一致；
- 发布后健康检查和监控正常，且不存在需要立即回滚的信号。

## 7. 原 11 个门禁与三阶段映射

| 原门禁顺序 | 门禁 | 新阶段 |
|---:|---|---|
| 1 | `freeze` | 阶段一 |
| 2 | `candidate-smoke` | 阶段一 |
| 3 | `formal-eval` | 阶段一 |
| 4 | `image-supply-chain` | 阶段二 |
| 5 | `staging-deploy` | 阶段二 |
| 6 | `governance-drill` | 阶段二 |
| 7 | `staging-e2e` | 阶段二 |
| 8 | `operations-security` | 阶段三 |
| 9 | `load-recovery` | 阶段三 |
| 10 | `final-go-no-go` | 阶段三 |
| 11 | `github-release` | 阶段三 |

阶段一的“冻结前仓库能力收口”是 `freeze` 的前置条件，不新增、删除或重排以上 11 个门禁。

## 8. 当前立即行动

1. 人工审核并合并 Production 对象存储代码基础 PR；该 PR 不授权真实 Bucket 写入、付费、部署或其他外部变更，`P8-001` 保持 `Open`。
2. PR 的四项必需 CI 成功并合并后，在本地更新到新的干净 `main`，继续阶段一的“冻结前仓库能力收口”。
3. Production 对象存储适配器代码基础合并后，下一项实现用户导出/级联删除，再依次处理 MFA、append-only 审计、Provider/Region 门禁和独立执行面部署契约；每项单独准备、验证并停在人工 PR。
4. 所有计划内代码 PR 合并后，再在仓库外准备 9 个私有 Freeze 输入，备份当前部分填写的私有计划，并通过初始化器重新生成绑定最终 HEAD 的计划。
5. 运行填充计划预检；通过后单独申请候选 smoke 的数据与费用授权。
6. 后续严格按阶段一→阶段二→阶段三推进；冻结后的任何仓库修改都会使候选证据失效并返回阶段一。
